import React from 'react';
import { Layout } from 'antd';
import styles from './index.less';

const Rules: React.FC = () => {
  return (
    <Layout className={styles.mainContent}>
      <h1>Quality Rules Management & Batch Dispose</h1>
    </Layout>
  );
};

export default Rules;
