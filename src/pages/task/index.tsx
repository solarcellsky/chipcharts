import React from 'react';
import { Layout } from 'antd';
import styles from './index.less';

const Task: React.FC = () => {
  return (
    <Layout className={styles.mainContent}>
      <h1>Task & Alteration</h1>
    </Layout>
  );
};

export default Task;
